# Live server data & uploads (lives only on server, never overwrite)
database.sqlite*
uploads/
uploads/**
admin/landing_pages/*.html
admin/landing_pages/*.json
admin/landing_pages/backups/
landing_pages/
.DS_Store
